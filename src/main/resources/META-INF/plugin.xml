<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin url="http://github.com/vsch/SimpleSerialPortService" require-restart="true">
    <!-- Unique identifier of the plugin. It should be FQN. It cannot be changed between the plugin versions. -->
    <id>com.vladsch.plugins.SimpleSerialPortService</id>

    <!-- Public plugin name should be written in Title Case.
         Guidelines: https://plugins.jetbrains.com/docs/marketplace/plugin-overview-page.html#plugin-name -->
    <name>SimpleSerialPortService</name>

    <vendor email="vladimir.schneider@gmail.com" url="https://github.com/vsch">Vladimir Schneider</vendor>

    <!-- Description of the plugin displayed on the Plugin Page and IDE Plugin Manager.
         Simple HTML elements (text formatting, paragraphs, and lists) can be added inside of <![CDATA[ ]]> tag.
         Guidelines: https://plugins.jetbrains.com/docs/marketplace/plugin-overview-page.html#plugin-description -->
    <description><![CDATA[
        <p>Exposes <a href="https://github.com/java-native/jssc">jscc</a> serial library as a service to allow multiple plugins to share the library and
            provide serial port connectivity without conflicting with each other.</p>
        <p>To make the service compatible with now JetBrains maintained <code>Serial Monitor</code> plugin, the
            service and associated interfaces were copied into this plugin. If <code>Serial Monitor</code> plugin is
            installed then this plugin&rsquo;s <code>SerialPortManager</code> will delegate all calls to the <code>Serial Monitor</code>
            plugin&rsquo;s <code>JsscSerialService</code>. If the <code>Serial Monitor</code> plugin is not installed, then this plugin
            will provide the service via the <a href="https://github.com/java-native/jssc">jscc</a> library in this plugin.</p>
        <p>Effectively, any plugin can implement serial monitoring by including this plugin as a dependency
            and seamlessly provide serial port communication with or without <code>Serial Monitor</code> being
            installed or compatible with an earlier version of the IDE.</p>
        <hr>
        <p>This plugin is intended to be installed as a dependency of other plugins since it exposes no
            user functionality of its own.</p>
        <hr>
        <p>| <a href="https://github.com/vsch/SimpleSerialPortService/issues">Issues</a> | <a href="https://github.com/vsch/SimpleSerialPortService">Plugin Source</a> |</p>
        ]]></description>

    <!-- Product and plugin compatibility requirements.
         Read more: https://plugins.jetbrains.com/docs/intellij/plugin-compatibility.html -->
    <depends>com.intellij.modules.platform</depends>
    <!--<depends>SerialPortMonitor</depends>-->
    <depends optional="true" config-file="plugin-serial-monitor.xml">SerialPortMonitor</depends>

    <!-- Extension points defined by the plugin.
         Read more: https://plugins.jetbrains.com/docs/intellij/plugin-extension-points.html -->
    <extensions defaultExtensionNs="com.intellij">
        <applicationService serviceImplementation="com.vladsch.plugins.simpleserialportservice.JsscSerialServiceImpl" serviceInterface="com.vladsch.plugins.simpleserialportservice.JsscSerialService"/>
    </extensions>
</idea-plugin>
